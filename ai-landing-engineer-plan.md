# AI 落地工程师技能学习规划

> 日期：2026-08-19
> 背景：前端（React/Electron/Capacitor）+ 已有本地部署实战（DGX Spark + DeepSeek）
> 目标：成为"私有化 AI 落地"全栈 —— 能把大模型装进内网、让业务人员真正用起来
> 对标岗位：AI 应用工程师 / 本地化部署工程师 / AI 前端工程师 / 行业解决方案工程师

---

## 1. 技能地图（四级）

```text
┌─ 第一层：AI 应用核心（必学，市场 T1）
│   ├─ LLM API 集成（OpenAI 兼容协议）
│   ├─ 提示词工程（含结构化输出 JSON Schema）
│   ├─ 流式输出（SSE / WebSocket）
│   └─ 输出可靠性（解析容错、降级、重试、评估）
│
├─ 第二层：落地工程（市场 T2）
│   ├─ RAG（向量检索 + 文档切分 + 引用）
│   ├─ Docker 容器化部署
│   ├─ 模型服务化（vLLM / Ollama / llama.cpp）
│   ├─ 上下文工程（token 预算、缓存、压缩）
│   └─ 成本优化（模型路由、缓存命中）
│
├─ 第三层：行业/系统集成（市场 T3，溢价）
│   ├─ 私有化部署（GPU 服务器、内网、离线）
│   ├─ 安全合规（数据不出域、加密、审计）
│   ├─ 前端 AI 交互（流式渲染、Diff、定位高亮）
│   └─ 行业知识（医疗/法律/政务任一）
│
└─ 第四层：稀缺溢价（可选）
    ├─ 模型微调（LoRA / QLoRA）
    ├─ Agent / 工具调用（Function Calling）
    ├─ 多模型路由 / 评估体系
    └─ GPU 运维（显存、并发、故障）
```

## 2. 已具备（摸底自评）

```text
✅ 前端：React / Electron / TypeScript / Capacitor
✅ 本地部署：DGX Spark + DeepSeek v4 flash 实战（病历预检）
✅ API 集成基础：OpenAI 兼容 chat 接口
✅ 安全基线：safeStorage、权限模型、审计（Molecraft/oneapp 经验）

❌ 待补：SSE 流式、结构化输出、RAG、Docker、vLLM/Ollama
```

自测题：

```text
1. 能不能手写 fetch 流式解析（读 chunk 而不是等全部返回）？
2. 能不能让模型返回固定 JSON 并可靠解析？
3. 能不能用 Docker 起 Nginx + 后端？
```

## 3. 学习路径（按周，每天 2 小时）

### 阶段 1：AI 应用核心（第 1~3 周）

```text
第 1 周：LLM API 深度
  1. 用 Node/TS 手写 OpenAI 兼容客户端（不用 SDK）
  2. 流式输出：response.body reader → 解析 SSE → 增量渲染
  3. 结构化输出：prompt 要求 JSON → parse + 容错
  4. 项目：把病历检查改成"流式 + 结构化输出"版

第 2 周：提示词工程
  1. 角色 / 示例 / CoT / 输出约束
  2. 写"质量评估提示词"迭代 3 版（对比输出质量）
  3. tokenizer 估算成本（tiktoken 或对应实现）

第 3 周：可靠性工程
  1. 超时 / 重试 / 限流（指数退避）
  2. 输出校验（JSON Schema + 失败降级）
  3. 简单评估：造 20 条测试病历，记录准确率
```

### 阶段 2：落地工程（第 4~7 周）

```text
第 4 周：Docker
  1. Dockerfile 打包 Node 后端 + React 前端
  2. docker-compose 起 Nginx + 后端 +（可选）向量库
  3. 目标：一条命令在另一台机器跑起来

第 5 周：模型本地服务化
  1. Ollama（最简单）→ 本地跑 qwen/llama
  2. vLLM（生产级）→ 起 OpenAI 兼容服务
  3. 对比本地模型 vs 云端 API 的质量/速度
  4. 项目：病历检查后端指向本地 vLLM

第 6~7 周：RAG
  1. 文档切分（chunking 策略）
  2. 向量化 + 检索（Chroma / pgvector）
  3. 检索结果注入提示词 + 引用溯源
  4. 项目：《病历书写规范》RAG，回答带引用
```

### 阶段 3：行业集成（第 8~10 周）

```text
第 8 周：前端 AI 交互进阶
  1. 流式打字机效果（增量渲染 + 防抖）
  2. 长文本 Diff（diff-match-patch）
  3. 原文定位高亮（quote 模糊匹配）

第 9 周：安全合规
  1. API key 安全（safeStorage / 环境变量 / KMS）
  2. 审计日志 + 防篡改（哈希链）
  3. 数据不出域方案（内网 + 无外网依赖）

第 10 周：综合项目（交付作品集）
  病历检查完整交付：
  粘贴/上传 → 流式检查 → 缺陷高亮 → 导出报告 → Docker 一键部署
```

### 阶段 4（可选）：溢价技能

```text
Agent / Function Calling（1 周）：
  模型能调用工具（读文件、查库）→ Molecraft 方向

模型微调（2 周，有 GPU 时）：
  LoRA 微调让模型更懂病历术语

GPU 运维（随用随学）：
  vLLM 并发、显存优化、故障排查
```

## 4. 阶段作品清单（简历素材）

```text
阶段 1：流式病历检查器（后端手写）
阶段 2：Docker 一键部署 + 本地 vLLM 版
阶段 3：完整交付包（安全、导出、部署）
阶段 4：Agent 版（能调用工具的检查器）

简历示例：
  "基于本地 vLLM 的医疗病历质控系统：
   流式结构化输出、RAG 引用溯源、Docker 私有化部署、
   safeStorage 密钥管理、审计防篡改"
```

## 5. 学习资源（精选）

```text
1. 官方文档（第一手）：
   - OpenAI API 文档（chat/completions 流式）
   - vLLM / Ollama 文档
   - Docker 官方教程

2. 拆一个开源项目：
   - Chatbot UI 类 LLM 聊天前端 → 读流式、状态管理、错误处理

3. 社区：
   - RAG 最佳实践（chunking、rerank）
   - 不报培训班，动手项目 > 一切课程
```

## 6. 时间与优先级

```text
每周 10 小时（周末集中）：
  2 个月   → 阶段 1+2（能独立交付"本地 AI 应用"）
  3 个月   → 阶段 3（能交付"行业私有化方案"）
  4~5 个月 → 阶段 4 入门（Agent/微调）

优先级铁律：
  流式 + 结构化输出 + Docker + 本地模型  → 先学（最常用）
  RAG + 安全 + 前端交互                   → 次之（行业落地）
  Agent + 微调                            → 最后（溢价非必需）
```

## 7. 面试/报价差异化卖点

```text
"我能把大模型装进你的内网，并且让业务人员真正用起来"
（部署 + 集成 + 界面 + 信任，一条龙）

简历不写"调了 API"，写"本地部署 DeepSeek 实现病历预检，
缺陷定位准确率 X%"——用真实经历做作品集。

面试核心话术：
  "我不仅能调通 API，还能让它在内网跑、让人愿意用、
   出了问题能追责"
```
